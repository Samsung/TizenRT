if (CONFIG_CKMN)
	list(APPEND incs ckmn)
	list(APPEND srcs ckmn/ckmn_driver.c)
	if (CONFIG_CKMN_TEST)
		list(APPEND srcs ckmn/ckmn_test.c)
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
endif()
