/* == "version" + "beken_trustzone pr-xxx "compile date" + "compile time"" == */

== version beken_trustzone pr-209 2026/04/21-17:28:00==
   
   1. solve the problem of ss decryption failure
     The key read from otp does not perform big-endian conversion
   2. update the flash config of XMC type

== version beken_trustzone pr-211 2026/04/22-17:25:00==

   1. let the TFM dump log can be controlled by the secure level
     The secure world provides a service interface, and non-secure registered callbacks pass in
the value of the current secure level.
   2. Optimize the ld file
     Fix the name of a file in ld.

== version beken_trustzone pr-213 2026/04/28-15:58:00==
   
   1. Update swd status acquisition implementation

== version beken_trustzone pr-214 2026/04/30-10:39:00==

   1. fix the bp crc calculation fail
      Due to Samsung's update to the BP data structure, the CRC verification of BP in the bootloader fails. We now need to fix the issue of BP CRC check failure.

== version beken_trustzone pr-221 2026/05/21-10:50:00==

   1. TFM access flash with lock protection
      Operate flash in tfm will set flash controller as security status. If non-secure world operate flash controller at the same time, it will trigger bus fault.

== version beken_trustzone pr-223 2026/06/04-15:53:38==

   1. flash improvement about set protect type within flash erase/write api
    - The original application-layer operations for adding/de-protecting and erasing/writing are now implemented within the erase/write interface, ensuring atomic operations within the flash driver API;
    - Configure the WP and flash SRP status registers for hardware protection of the flash status registers;
    - Write to the status register using volatile mode.


== version beken_trustzone pr-224 2026/06/15-17:44:00==

   1.Bootloader & TFM via BP Partition parse the kernel address
    - Description of BP Partition Update and Realization of Kernel Address Parsing and Retrieval by Bootloader & TFM via BP Partition
	- add build version in bootloader
	- Fix the reason field of test_case3, change its value from 4 to 2.
   2.Fix GCM AAD padding when plaintext length is zero
